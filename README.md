## Initial Release

* Added Echo World GitHub Action
* Supports optional `name` input
* Defaults to `"World"` when no input is provided
* Implemented using Node.js 24 runtime
* Dependencies bundled for direct execution (no installation required)

### Usage

```yaml
- uses: Hariharan482/hello-world-action@v2
  with:
    name: <name>
```
